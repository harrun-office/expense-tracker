[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[context.production.environment]
  # I recommend setting VITE_API_URL in Netlify UI instead of committing secrets.
  VITE_API_URL = "https://expense-tracker-uvb7.onrender.com/"

# SPA catch-all redirect — do NOT force, so static files are served normally
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200